{
  "name": "@chs/contract",
  "version": "0.1.0",
  "private": true,
  "description": "The API in one place: every endpoint, its schemas, access rule, cache invalidation and realtime events. Backend and clients both read from here.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "oxlint src"
  },
  "peerDependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "zod": "^4.1.0"
  }
}
