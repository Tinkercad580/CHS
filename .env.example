# docker compose settings. Generate each secret with: openssl rand -base64 48
POSTGRES_PASSWORD=
JWT_ACCESS_SECRET=
JWT_RESTRICTED_SECRET=
DATA_ENCRYPTION_KEY=
CORS_ORIGINS=http://localhost:8080
PAYMENT_WEBHOOK_SECRET=
# Push and email — see docs/NOTIFICATIONS.md
PUSH_PROVIDER=log
FIREBASE_SERVICE_ACCOUNT_FILE=
MAIL_PROVIDER=log
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
